package unet.torrent.trackers.servers;

public class UDPTracker {

    public UDPTracker(){
        //DECODE PACKET
        //GET PACKET & CHECK IF FIRST 8 bytes == MAGIC CONSTANT PROTOCOL_ID
        //PARSE ACTION

        /*
        action = (((buf [0] & 0xff) << 24)
            | ((buf [1] & 0xff) << 16)
            | ((buf [2] & 0xff) << 8)
            | (buf [3] & 0xff));
        */

        //DECODE TID
    }
}
